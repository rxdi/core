export declare class LoggerConfig {
    logging?: boolean;
    hashes?: boolean;
    date?: boolean;
    exitHandler?: boolean;
    fileService?: boolean;
}
export declare class ConfigModel {
    init?: boolean;
    logger?: LoggerConfig;
}
